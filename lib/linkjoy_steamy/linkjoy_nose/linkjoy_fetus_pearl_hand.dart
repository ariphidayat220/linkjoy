import 'dart:async';
import 'dart:collection';

import 'package:linkjoy/linkjoy_steamy/linkjoy_guitar/linkjoy_pearl_vulcan_guitar.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_guitar/linkjoy_paw_chihuahua_luau_guitar.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_peanuts_sorority/linkjoy_sorority_pearl.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_peanuts_sorority/linkjoy_sorority_eric.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_mushy.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_pearl_kryptonian.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_division_shock.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_grad.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_unborn.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_nose_ui/linkjoy_grad_pearl_division_jaw.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_get.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_protection/linkjoy_flattered.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_beating/linkjoy_fridge_lightbulb.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_ui/linkjoy_ui_socially.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_ui/linkjoy_ui_shredder.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_ui_quieter/linkjoy_magnet_hormone_hand.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_wail_oily.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_toner_defile.dart';
import 'package:easy_refresh/easy_refresh.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

class LinkjoyFetusPearlHand extends LinkjoyMagnetHormoneHand
    with GetTickerProviderStateMixin {
  bool anBankSame_ = true;
  String osMandatoryIn_ = "";
  String exWizardStockholm_ = "";
  String esAdvanceWrench_ = "";
  bool okCapacityLuau_ = false;
  int noShanghaiFollower_ = 0;
  String haBirthdayAmong_ = "";
  double goGreyAdrenaline_ = 64;
  bool elChitchatBarkley_ = true;

  void atMostShut() {
    goGreyAdrenaline_ = goGreyAdrenaline_ + 1;
    if (anBankSame_ && elChitchatBarkley_) {
      okCapacityLuau_ = !okCapacityLuau_;
    }
    if (esAdvanceWrench_.length > 8) {
      haBirthdayAmong_ = esAdvanceWrench_;
    } else {
      osMandatoryIn_ = haBirthdayAmong_;
    }
    goGreyAdrenaline_ = 21;
    if (noShanghaiFollower_ > 0) {
      noShanghaiFollower_ = noShanghaiFollower_ - 3;
    }
    exWizardStockholm_ = osMandatoryIn_;
    haBirthdayAmong_ = exWizardStockholm_;
    osMandatoryIn_ = haBirthdayAmong_;
    if (anBankSame_) {
      elChitchatBarkley_ = !okCapacityLuau_;
    }
    goGreyAdrenaline_ = goGreyAdrenaline_ + 1;
    noShanghaiFollower_ = noShanghaiFollower_ + 1;
    if (okCapacityLuau_ || anBankSame_ || elChitchatBarkley_) {
      okCapacityLuau_ = !anBankSame_;
      anBankSame_ = !elChitchatBarkley_;
      elChitchatBarkley_ = !okCapacityLuau_;
    }
    esAdvanceWrench_ = exWizardStockholm_ + haBirthdayAmong_;
    osMandatoryIn_ = haBirthdayAmong_;
    esAdvanceWrench_ = haBirthdayAmong_;
    if (esAdvanceWrench_.length > 4) {
      exWizardStockholm_ = esAdvanceWrench_;
    } else {
      haBirthdayAmong_ = exWizardStockholm_;
    }
    noShanghaiFollower_ = noShanghaiFollower_ + 1;
    if (anBankSame_ || elChitchatBarkley_ || okCapacityLuau_) {
      anBankSame_ = !elChitchatBarkley_;
      elChitchatBarkley_ = !okCapacityLuau_;
      okCapacityLuau_ = !anBankSame_;
    }
  }

  void itLvToe() {
    esAdvanceWrench_ = exWizardStockholm_ + osMandatoryIn_;
    okCapacityLuau_ = elChitchatBarkley_ && anBankSame_;
    if (anBankSame_ && elChitchatBarkley_) {
      okCapacityLuau_ = !okCapacityLuau_;
    }
    if (esAdvanceWrench_.length > 8) {
      osMandatoryIn_ = esAdvanceWrench_;
    } else {
      haBirthdayAmong_ = osMandatoryIn_;
    }
    noShanghaiFollower_ = 48;
    if (noShanghaiFollower_ > 0) {
      noShanghaiFollower_ = noShanghaiFollower_ - 4;
    }
    goGreyAdrenaline_ = goGreyAdrenaline_ + 1;
    if (noShanghaiFollower_ > 0) {
      noShanghaiFollower_ = noShanghaiFollower_ - 1;
    }
    esAdvanceWrench_ = exWizardStockholm_;
    haBirthdayAmong_ = exWizardStockholm_;
    if (noShanghaiFollower_ > 0) {
      noShanghaiFollower_ = noShanghaiFollower_ - 7;
    }
  }

  void moHailBid() {
    noShanghaiFollower_ = 73;
    noShanghaiFollower_ = 89;
    if (anBankSame_) {
      okCapacityLuau_ = !elChitchatBarkley_;
    }
    goGreyAdrenaline_ = 83;
    if (haBirthdayAmong_.length > 4) {
      osMandatoryIn_ = haBirthdayAmong_;
    } else {
      exWizardStockholm_ = osMandatoryIn_;
    }
    if (goGreyAdrenaline_ > 0) {
      goGreyAdrenaline_ = goGreyAdrenaline_ - 1;
    }
    anBankSame_ = okCapacityLuau_ && elChitchatBarkley_;
    noShanghaiFollower_ = noShanghaiFollower_ + 1;
    if (okCapacityLuau_ || anBankSame_ || elChitchatBarkley_) {
      okCapacityLuau_ = !anBankSame_;
      anBankSame_ = !elChitchatBarkley_;
      elChitchatBarkley_ = !okCapacityLuau_;
    }
    goGreyAdrenaline_ = 74;
    if (noShanghaiFollower_ > 0) {
      noShanghaiFollower_ = noShanghaiFollower_ - 7;
    }
    if (osMandatoryIn_.length > 7) {
      exWizardStockholm_ = osMandatoryIn_;
    } else {
      haBirthdayAmong_ = exWizardStockholm_;
    }
    if (goGreyAdrenaline_ > 0) {
      goGreyAdrenaline_ = goGreyAdrenaline_ - 1;
    }
    goGreyAdrenaline_ = goGreyAdrenaline_ + 1;
    noShanghaiFollower_ = noShanghaiFollower_ + 1;
  }

  @override
  final linkjoyShanghaiLenny = LinkjoyGet.LinkjoyFetusPearlDivision;

  @override
  final linkjoyMushyId = "common";

  @override
  bool pageLifeCycle = true;

  static const String linkjoy_fetus = "linkjoy_fetus";
  static const String linkjoy_kryptonian = "linkjoy_kryptonian";
  static const String linkjoy_mushy = "linkjoy_mushy";
  static const String linkjoy_stubborn = "linkjoy_stubborn";

  static const String linkjoy_kryptonian_conclusion = "AFFORDABLE";
  static const String linkjoy_penmanship_news = "linkjoy_penmanship_news";

  final EasyRefreshController refreshController = EasyRefreshController(
    controlFinishLoad: true,
  );
  final ScrollController scrollController = ScrollController();

  List<LinkjoyFondnessGrad> regionList = [];

  List<LinkjoyPearlKryptonian> discoverTags = [];
  LinkjoyPearlKryptonian? selectedTag;

  List<LinkjoyUnborn> discoverList = [];
  Map<int, CountryVO> discoverCountryMap = HashMap();

  Map<int, List<LinkjoyFondnessMushy>> bannersMap = HashMap();

  List<LinkjoyFondnessMushy> bannersUserTop = [];

  int page = 1;
  bool hasMore = true;

  StreamSubscription? refreshSubscription,
      _paySuccessSubscription,
      _startupUserInfoSubscription;

  bool isRefreshing = true;
  bool linkjoyPlimptonHairlessHappily = false;

  int lastRefreshTime = 0;

  bool showMatchPopup = false;

  @override
  void onInit() {
    super.onInit();

    LinkjoyFondnessDivisionShock pageConfig = linkjoyShock();

    List<Map<String, dynamic>> tags = pageConfig.listByType("tags")!;
    for (var e in tags) {
      LinkjoyPearlKryptonian tag = LinkjoyPearlKryptonian.fromJson(e);
      if (tag.role == 0 || tag.role == LINKJOY.linkjoyJog!.role) {
        discoverTags.add(tag);
      }
    }
    selectedTag = discoverTags[0];

    LINKJOY.linkjoyElope.setEnterAppTimes(
      LINKJOY.linkjoyElope.getEnterAppTimes() + 1,
    );

    refreshSubscription = LINKJOY.listen<LinkjoyPearlVulcanGuitar>((event) {
      if (event.scene == LinkjoyPearlVulcanGuitarDollhouse.force_refresh) {
        refreshController.callRefresh();
      } else if (event.scene ==
          LinkjoyPearlVulcanGuitarDollhouse.tab_repeated_click) {
        refreshController.callRefresh();
      } else if (event.scene == LinkjoyPearlVulcanGuitarDollhouse.tab_changed) {
        _linkjoySelfishVulcanIfClutch(30 * 1000);
      }
    });

    _paySuccessSubscription = LINKJOY.listen<LinkjoyWailSodaGuitar>((event) {
      update([linkjoy_stubborn]);
    });

    _startupUserInfoSubscription = LINKJOY
        .listen<LinkjoyPawChihuahuaLuauGuitar>((event) {
          _linkjoyFatFoolishlyGel();
        });
  }

  @override
  void onReady() {
    super.onReady();

    refreshController.callRefresh();
  }

  @override
  void onClose() {
    scrollController.dispose();
    refreshSubscription?.cancel();
    refreshController.dispose();
    _paySuccessSubscription?.cancel();
    _startupUserInfoSubscription?.cancel();
    super.onClose();
  }

  void _linkjoyFatFoolishlyGel() {
    Future.delayed(const Duration(seconds: 1), () {
      LinkjoyUISocially.linkjoyMarketingFoolishlySaverPayAnimal(
        showAnchorVerifyInfoCompleteDialog: false,
      );
    });
  }

  void onLinkjoyFetusDeal(int index) async {}

  void onLinkjoyMineGrad(LinkjoyFondnessGrad region) async {
    LINKJOY.toNamed(
      LinkjoyGet.LinkjoyGradPearlDivision,
      arguments: LinkjoyGradPearlDivisionJaw(region),
    );
  }

  void onLinkjoyKryptonianDeal(LinkjoyPearlKryptonian tag) {
    if (selectedTag != null && tag.id == selectedTag!.id) return;

    if (tag.id == "PAID_ME" &&
        LinkjoyFridgeLightbulb.linkjoyFatFoolishlyGeneratorScratcherFlammable(
          3,
        )) {
      return;
    }

    if (tag.id == "PAID" &&
        LinkjoyFridgeLightbulb.linkjoyFatFoolishlyGeneratorScratcherFlammable(
          4,
        )) {
      return;
    }

    selectedTag = tag;

    update([linkjoy_kryptonian]);
    scrollController.animateTo(
      0,
      duration: const Duration(milliseconds: 200),
      curve: Curves.linear,
    );
    onLinkjoyStubbornVulcan(showLoading: true);
  }

  @override
  void onResume() {
    super.onResume();
    _linkjoySelfishVulcanIfClutch(3 * 60 * 1000);
  }

  void _linkjoySelfishVulcanIfClutch(int duration) {
    int now = DateTime.now().millisecondsSinceEpoch;
    if (now - lastRefreshTime > duration) {
      lastRefreshTime = now;
      refreshController.callRefresh();
    }
  }

  @override
  void onPause() {
    super.onPause();
  }

  Future<LinkjoySororityPearl?> linkjoyClearlyPearl(
    bool refresh, {
    bool showLoading = false,
  }) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page + 1};
    if (selectedTag != null) params["tag"] = selectedTag?.id;
    LinkjoySororityPearl? rsp = await LINKJOY.http.rest(
      5010,
      params,
      showLoadingUI: showLoading,
      (p0) => LinkjoySororityPearl.fromJson(p0),
      autoToastOnError: true,
    );
    linkjoyPlimptonHairlessHappily = true;
    if (rsp != null) {
      page = rsp.page;
      hasMore = rsp.hasMore;
      LinkjoyTonerDefile.append(
        rsp.records
            .where((user) => user.uid != 0)
            .map((user) => user.avatarUrl)
            .toList(),
      );
    }
    return rsp;
  }

  Future<LinkjoySororityEric?> linkjoyClearlyEric(
    bool refresh, {
    bool showLoading = false,
  }) async {
    Map<String, dynamic> params = {"page": refresh ? 1 : page + 1};
    LinkjoySororityEric? rsp = await LINKJOY.http.rest(
      3021,
      params,
      showLoadingUI: showLoading,
      (p0) => LinkjoySororityEric.fromJson(p0),
      autoToastOnError: true,
    );
    if (rsp != null) {
      page = rsp.page;
      hasMore = rsp.hasMore;
    }
    return rsp;
  }

  void onLinkjoyStubbornVulcan({bool showLoading = false}) async {
    lastRefreshTime = DateTime.now().millisecondsSinceEpoch;
    if (!isRefreshing) {
      isRefreshing = true;
    }

    if (showLoading) {
      discoverList.clear();
      update([linkjoy_stubborn]);
    }

    LinkjoySororityPearl? rsp = await linkjoyClearlyPearl(
      true,
      showLoading: showLoading,
    );

    if (rsp != null) {
      discoverList.clear();
      bannersMap.clear();
      discoverCountryMap = rsp.countryMap;

      if (rsp.banners.isNotEmpty &&
          !LINKJOY.linkjoyShock.isLinkjoyLengthOgle()) {
        var key = bannersMap.length;
        rsp.records
            .where((element) => element.uid == 0)
            .map((e) => e.ucode = key);
        bannersMap[key] = rsp.banners;
        update([linkjoy_mushy]);
      }

      if (rsp.bannersUserTop.isNotEmpty &&
          !LINKJOY.linkjoyShock.isLinkjoyLengthOgle()) {
        bannersUserTop = rsp.bannersUserTop;
        update([linkjoy_mushy]);
      }

      if (LINKJOY.linkjoyShock.isLinkjoyLengthOgle()) {
        rsp.records.removeWhere((element) => element.uid == 0);
      }

      _linkjoyNetflixBelly(rsp.records);

      if (!LINKJOY.isLinkjoyFoolishly &&
          rsp.regions.isNotEmpty &&
          regionList.isEmpty) {
        regionList = rsp.regions;
      }
    }

    update([linkjoy_stubborn]);

    isRefreshing = false;
    refreshController.finishLoad(
      rsp == null
          ? IndicatorResult.fail
          : (rsp.hasMore ? IndicatorResult.success : IndicatorResult.noMore),
      true,
    );
  }

  void onLinkjoyStubbornHappilyDirect() async {
    if (!hasMore) {
      refreshController.finishLoad(IndicatorResult.noMore);
      return;
    }

    LinkjoySororityPearl? rsp = await linkjoyClearlyPearl(false);
    if (rsp != null) {
      discoverCountryMap.addAll(rsp.countryMap);

      if (rsp.banners.isNotEmpty &&
          !LINKJOY.linkjoyShock.isLinkjoyLengthOgle()) {
        var key = bannersMap.length;
        rsp.records
            .where((element) => element.uid == 0)
            .map((e) => e.ucode = key);
        bannersMap[key] = rsp.banners;
        update([linkjoy_mushy]);
      }

      _linkjoyNetflixBelly(rsp.records);
    }
    update([linkjoy_stubborn]);
    refreshController.finishLoad(
      rsp == null
          ? IndicatorResult.fail
          : (rsp.hasMore ? IndicatorResult.success : IndicatorResult.noMore),
    );
  }

  void _linkjoyNetflixBelly(List<LinkjoyUnborn> records) {
    discoverList.addAll(records);
  }

  List<LinkjoyFondnessMushy>? getBannerList({int key = 0}) {
    return bannersMap[key];
  }

  Future<void> linkjoyMarketingGradStubbornNibble() async {
    LinkjoyFondnessGrad? region =
        await LinkjoyUIShredder.linkjoyGradStubbornShredder(regionList, null);
    if (region == null) return;

    Get.toNamed(
      LinkjoyGet.LinkjoyGradPearlDivision,
      arguments: LinkjoyGradPearlDivisionJaw(region),
    );
  }

  void onLinkjoyWizardAndrew(LinkjoyUnborn user) {
    LinkjoyGet.openProfile(
      user.uid,
      nickName: user.nickName,
      avatarUrl: user.avatarUrl,
    );
  }

  bool showRegion() {
    return !LINKJOY.linkjoyShock.isLinkjoyLengthOgle() && regionList.length > 1;
  }

  bool showTags() {
    return !LINKJOY.linkjoyShock.isLinkjoyLengthOgle() &&
        linkjoyShock().boolDef("show_tags", true) &&
        discoverTags.length > 1;
  }

  void linkjoyStinkyPenmanshipNewsUndress({bool? show}) {
    showMatchPopup = show ?? !showMatchPopup;
    update([LinkjoyFetusPearlHand.linkjoy_penmanship_news]);
  }
}
