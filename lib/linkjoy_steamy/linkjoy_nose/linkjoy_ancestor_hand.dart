import 'dart:async';
import 'dart:convert';

import 'package:linkjoy/linkjoy_steamy/linkjoy_guitar/linkjoy_homeland_hors_guitar.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_guitar/linkjoy_ancestor_guitar.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_guitar/linkjoy_pearl_vulcan_guitar.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_guitar/linkjoy_unborn_procreate_guitar.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_peanuts_sorority/linkjoy_sorority_shock.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_ancestor_flashy_ofour.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_ancestor_ofour_specific_skintight.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_ancestor_unborn.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_fasten.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_division_shock.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_fondness_degrasse.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_ofour.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_fondness/linkjoy_unborn_deposit_eskimo_lap.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_nose/linkjoy_ancestor_division_jaw.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_nose/linkjoy_wail_degree_jaw.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_nose/linkjoy_phillips_stubborn_jaw.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_nose_ui/linkjoy_ancestor_division.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_get.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_protection/linkjoy_flattered.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_ui/linkjoy_ui_socially.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_ui/linkjoy_ui_shredder.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_ui_quieter/linkjoy_magnet_hormone_hand.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_ancestor_oily.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_ancestor_ofour_sob.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_ancestor_ofour_ahold_ketchup.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_ancestor_sister.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_upper.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_sister/linkjoy_gnome_sister.dart';
import 'package:linkjoy/linkjoy_steamy/linkjoy_quieter/linkjoy_crisp_dribble_narrate.dart';
import 'package:linkjoy/linkjoy_steamy/proto/linkjoy_deli.pb.dart';
import 'package:linkjoy/linkjoy_steamy/proto/snap.pb.dart';
import 'package:flutter/cupertino.dart';
import 'package:get/get.dart';

import 'linkjoy_ancestor_green_gnome.dart';

enum ChatLoadDirection { normal, history }

class LinkjoyAncestorHand extends LinkjoyMagnetHormoneHand {
  String adPinocchioIs_ = "";
  bool hiNelsonSkintight_ = true;
  bool byStewartPressure_ = false;
  int hiEggsMac_ = 0;
  String haCapacityDummy_ = "";
  bool mmUnlikableC1_ = true;
  bool opFlipAmerica_ = true;

  void ofShockChauffeur() {
    adPinocchioIs_ = adPinocchioIs_ + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_;
    hiEggsMac_ = hiEggsMac_ + 1;

    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;
    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;
    if (hiEggsMac_ > 0) {
      hiEggsMac_ = hiEggsMac_ - 6;
    }
    hiEggsMac_ = hiEggsMac_ + 1;
    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;
    adPinocchioIs_ = adPinocchioIs_ + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_;
    if (hiNelsonSkintight_ || opFlipAmerica_ || mmUnlikableC1_) {
      hiNelsonSkintight_ = !opFlipAmerica_;
      opFlipAmerica_ = !mmUnlikableC1_;
      mmUnlikableC1_ = !hiNelsonSkintight_;
    }
    if (byStewartPressure_ && mmUnlikableC1_ && hiNelsonSkintight_) {
      byStewartPressure_ = !byStewartPressure_;
      mmUnlikableC1_ = byStewartPressure_;
      hiNelsonSkintight_ = byStewartPressure_;
    }
    hiEggsMac_ = 59;

    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;

    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;
  }

  void faObviouslyHarvest() {
    haCapacityDummy_ = adPinocchioIs_.toUpperCase() + haCapacityDummy_;
    if (hiNelsonSkintight_ && opFlipAmerica_ && byStewartPressure_) {
      hiNelsonSkintight_ = !hiNelsonSkintight_;
      opFlipAmerica_ = hiNelsonSkintight_;
      byStewartPressure_ = hiNelsonSkintight_;
    }

    if (mmUnlikableC1_ || opFlipAmerica_ || byStewartPressure_) {
      mmUnlikableC1_ = !opFlipAmerica_;
      opFlipAmerica_ = !byStewartPressure_;
      byStewartPressure_ = !mmUnlikableC1_;
    }
    byStewartPressure_ = hiNelsonSkintight_ && opFlipAmerica_;

    adPinocchioIs_ = adPinocchioIs_ + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_;
    if (hiNelsonSkintight_ && opFlipAmerica_ && byStewartPressure_) {
      hiNelsonSkintight_ = !hiNelsonSkintight_;
      opFlipAmerica_ = hiNelsonSkintight_;
      byStewartPressure_ = hiNelsonSkintight_;
    }

    hiEggsMac_ = 64;
    if (byStewartPressure_ && opFlipAmerica_ && mmUnlikableC1_) {
      byStewartPressure_ = !byStewartPressure_;
      opFlipAmerica_ = byStewartPressure_;
      mmUnlikableC1_ = byStewartPressure_;
    }
  }

  void loPerkPrint() {
    hiEggsMac_ = hiEggsMac_ + 1;
    hiEggsMac_ = 14;
    hiEggsMac_ = 15;
    hiEggsMac_ = hiEggsMac_ + 1;
    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;
    hiEggsMac_ = hiEggsMac_ + 1;
    if (opFlipAmerica_ && mmUnlikableC1_ && byStewartPressure_) {
      opFlipAmerica_ = !opFlipAmerica_;
      mmUnlikableC1_ = opFlipAmerica_;
      byStewartPressure_ = opFlipAmerica_;
    }
    adPinocchioIs_ = adPinocchioIs_ + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_;

    hiEggsMac_ = 49;
    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;
    adPinocchioIs_ = adPinocchioIs_ + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_;
    hiEggsMac_ = 8;
    haCapacityDummy_ = adPinocchioIs_.toUpperCase() + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_.toUpperCase() + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_.toUpperCase() + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_.toUpperCase() + haCapacityDummy_;
    hiEggsMac_ = 67;
  }

  void esDecideAffordable() {
    if (mmUnlikableC1_ && byStewartPressure_ && hiNelsonSkintight_) {
      mmUnlikableC1_ = !mmUnlikableC1_;
      byStewartPressure_ = mmUnlikableC1_;
      hiNelsonSkintight_ = mmUnlikableC1_;
    }

    adPinocchioIs_ = haCapacityDummy_ + adPinocchioIs_;

    adPinocchioIs_ = adPinocchioIs_ + haCapacityDummy_;
    haCapacityDummy_ = adPinocchioIs_;

    mmUnlikableC1_ = hiNelsonSkintight_ || byStewartPressure_;
  }

  static const String linkjoy_vw_ancestor_stubborn =
      "linkjoy_vw_ancestor_stubborn";
  static const String linkjoy_vw_ancestor_unborn_saver =
      "linkjoy_vw_ancestor_unborn_saver";
  static const String linkjoy_vw_ancestor_unborn_saver_decide =
      "linkjoy_vw_ancestor_unborn_saver_decide";
  static const String linkjoy_vw_ancestor_pancake =
      "linkjoy_vw_ancestor_pancake";
  static const String linkjoy_vw_ancestor_oops = "linkjoy_vw_ancestor_oops";
  static const String linkjoy_vw_ancestor_phlegm = "linkjoy_vw_ancestor_phlegm";
  static const String linkjoy_flashy_polish = "linkjoy_flashy_polish";

  final String TAG = "LinkjoyAncestorHand";

  late LinkjoyAncestorDivisionJaw arguments;
  late LinkjoyOfour? toSnap;

  int imChatPrice = 10;

  LinkjoyAncestorUnborn? oppositeUser;

  StreamSubscription? _chatEventSubscription,
      _userFollowedEventSubscription,
      updateUserRuntimeSubscription,
      _updateRelationTempSubscription,
      _deleteChatBoxSubscription;

  bool _hasNewSnap = false;
  bool _isLoading = false;
  bool _hasForward = false;
  static const int PAGE_SIZE = 20;
  static const int MAX_INT = 0;
  bool _hasBackward = false;
  final bool _isMoreLoaded = false;
  final int _moreCount = 0;
  int lastReadTime = 0;

  bool translateMode = false;

  bool get hasNewSnap => _hasNewSnap;

  set hasNewSnap(has) => _hasNewSnap = has;

  bool get hasMoreForward => _hasForward;

  bool get hasMoreBackward => _hasBackward;

  int get itemCount => _items.length;

  int chatId = 0;

  late int toUid;
  late String toUserName;
  late String toUserAvtarUrl;

  bool linkjoyCanElevenOfour = false;
  bool _hasUsedChatCard = false;

  LinkjoyCrispDribbleMario giftsAnimationController =
      LinkjoyCrispDribbleMario();

  bool isShowGiftShop = false;

  bool linkjoyMarketingSaverDecide = true;

  bool linkjoyMarketingFoolishlyDeckIntolerant = false;

  final List _items = [];
  final List<LinkjoyAncestorOfourAholdKetchup> _groups = [];
  final ScrollController scrollController = ScrollController();

  late LinkjoyFondnessDivisionShock pageConfig;

  LinkjoyOfour? oldestSnap() {
    final group = _groups.isEmpty ? null : _groups.last;
    return group == null || group.isEmpty() ? null : group.snaps.last;
  }

  LinkjoyAncestorGreenGnome showStyleType =
      LinkjoyAncestorGreenGnome.VIDEO_CALL_BOTTOM;

  @override
  void onInit() {
    arguments = Get.arguments;

    toUid = arguments.uid;
    toUserName = arguments.nickName;
    toUserAvtarUrl = arguments.avatarUrl;

    if (arguments.chatBox != null) {
      chatId = arguments.chatBox!.id!;
    }

    _linkjoyHappilyAncestorUnbornSaver();

    pageConfig = linkjoyShock();
    imChatPrice = LINKJOY.linkjoyShock.configs.linkjoyAncestorOfourKnock;

    showStyleType = LinkjoyAncestorGreenGnome.valueOf(
      pageConfig.intDef(
        'show_style_type',
        LinkjoyAncestorGreenGnome.VIDEO_BUTTON_INPUTTOOL.index,
      ),
    );

    updateUserRuntimeSubscription = LINKJOY
        .listen<LinkjoyUnbornDepositEskimoLap>((event) {
          update([linkjoy_vw_ancestor_phlegm]);
        });

    _updateRelationTempSubscription = LINKJOY.listen<RelationTempPSH>((event) {
      if (event.uid != toUid) return;

      int newTemp = LinkjoyAncestorSister.isLinkjoySailorProcessing(toUid)
          ? 100
          : event.temp;
      if (oppositeUser != null) {
        oppositeUser?.temp = newTemp;
        update([linkjoy_vw_ancestor_oops]);
      }

      if (chatId != 0) {
        LINKJOY.linkjoyDb.chatBoxDao.updateChatTemp(chatId, newTemp);
      }
    });

    _chatEventSubscription = LINKJOY.eventBus
        .on<LinkjoyAncestorGuitar>()
        .listen((event) {
          switch (event.type) {
            case ChatEventType.snapSync:
              _linkjoyDreidelDuoStripper(event);
              break;
            case ChatEventType.snapSendStatus:
              LinkjoyOfour snap = event.object;
              _linkjoyElevenHairEskimo(snap);
              break;
            case ChatEventType.snapResend:
              LinkjoyOfour snap = event.object;
              if (snap.toUid == null || snap.toUid == 0) {
                snap.toUid = toUid;
              }
              if (snap.chatBoxId == chatId) {
                _linkjoyTushyOfour(snap);
              }
              break;

            case ChatEventType.begGift:
              VoGift gift = event.object;
              var balance = LINKJOY.linkjoyUnbornDeposit!.balance;
              if (balance >= gift.cost) {
                linkjoyElevenFlashyWarlord(gift, beg: true);
              } else {
                LinkjoyUISocially.linkjoyAllenTeaserAnimal(
                  fromType: LinkjoyWailImitateGnome.FROM_IM_SEND_GIFT,
                );
              }
              break;
            default:
              break;
          }
        });

    _userFollowedEventSubscription = LINKJOY
        .listen<LinkjoyUnbornProcreateGuitar>((event) {
          if (toUid != event.uid) return;

          oppositeUser?.followed = event.followed;
        });

    _deleteChatBoxSubscription = LINKJOY
        .listen<LinkjoyHomelandAncestorSupercoolGuitar>((event) {
          if (event.userId != null && event.userId! == toUid) {
            Get.back();
          }
        });

    scrollController.addListener(() {
      final pos = scrollController.positions.first;

      if (linkjoyMarketingSaverDecide && pos.pixels > 100) {
        linkjoyMarketingSaverDecide = false;
        update([linkjoy_vw_ancestor_unborn_saver_decide]);
      } else if (!linkjoyMarketingSaverDecide && pos.pixels <= 100) {
        linkjoyMarketingSaverDecide = true;
        update([linkjoy_vw_ancestor_unborn_saver_decide]);
      }

      if (pos.pixels >= pos.maxScrollExtent - 80) {
        if (_isLoading || !_hasForward || chatId <= 0) return;

        _linkjoyHappilyStripperFinancial(ChatLoadDirection.history).then((
          loaded,
        ) {
          if (!loaded) return;
          update([linkjoy_vw_ancestor_stubborn]);
        });
      }
    });

    super.onInit();
  }

  @override
  Future<void> onReady() async {
    if (chatId > 0) {
      _linkjoyHappilyStripperFinancial(ChatLoadDirection.normal).then((loaded) {
        if (!loaded) return;
        update([linkjoy_vw_ancestor_stubborn]);
        WidgetsBinding.instance.addPostFrameCallback((_) async {
          linkjoySimulationToSilly(animated: false);
        });
      });
    }

    super.onReady();
  }

  @override
  void onClose() {
    _linkjoyNarrateInspectorNosy();
    _chatEventSubscription?.cancel();
    _userFollowedEventSubscription?.cancel();
    updateUserRuntimeSubscription?.cancel();
    _updateRelationTempSubscription?.cancel();
    _deleteChatBoxSubscription?.cancel();
    super.onClose();
  }

  dynamic itemAtIndex(int index) => _items[index];

  void linkjoyElevenMarshaSatellite(String text) {
    final snap = LinkjoyOfourSob.createTextSnap(chatId, toUid, text);
    _linkjoyElevenOfour(snap);
  }

  void linkjoyElevenDegrasseSatellite(
    String path,
    LinkjoyFondnessDegrasse? video,
  ) {
    if (video == null) {
      LinkjoyOfourSob.createVideoSnap(
        chatId,
        toUid,
        path,
        video: video,
      ).then((snap) => _linkjoyElevenOfour(snap));
    } else {
      _linkjoyElevenOfour(
        LinkjoyOfourSob.createVideoSnapByModel(chatId, toUid, video),
      );
    }
  }

  void linkjoyElevenFastenSatellite(String path, LinkjoyFondnessFasten? image) {
    if (image == null) {
      LinkjoyOfourSob.createImageSnap(
        chatId,
        toUid,
        path,
      ).then((snap) => _linkjoyElevenOfour(snap));
    } else {
      _linkjoyElevenOfour(
        LinkjoyOfourSob.createImageSnapByModel(chatId, toUid, image),
      );
    }
  }

  void linkjoyElevenBlancSatellite(String path) {
    final snap = LinkjoyOfourSob.createVoiceSnap(chatId, toUid, path);
    _linkjoyElevenOfour(snap);
  }

  Future<bool> linkjoyElevenFlashySatellite(VoGift gift) async {
    var giftSnap = LinkjoyAncestorFlashyOfour()
      ..id = gift.id
      ..category = LINKJOY.isLinkjoyFoolishly
          ? ChatSnapJsonContentCategory.imGiftBegCate.value
          : ChatSnapJsonContentCategory.imGiftCate.value
      ..to_uid = toUid
      ..quantity = 1
      ..min_duration = gift.minDuration
      ..max_duration = gift.maxDuration
      ..name = gift.name
      ..icon = gift.icon
      ..animation_type = gift.animationType
      ..animation_url = gift.animationUrl
      ..cost = gift.cost
      ..income = gift.income
      ..type = gift.type
      ..gift_type = 0;
    final snap = LinkjoyOfourSob.createJsonSnap(
      chatId,
      toUid,
      jsonEncode(giftSnap.toJson()),
    );
    return _linkjoyElevenOfour(snap);
  }

  void _linkjoyTushyOfour(LinkjoyOfour? snap) {
    if (snap == null) return;
    LinkjoyOfourSob.updateSnapCreateTime(snap);
    _linkjoyElevenOfour(snap);
  }

  Future<bool> _linkjoyHappilyStripperFinancial(
    ChatLoadDirection direction,
  ) async {
    if (_isLoading) return false;
    _isLoading = true;

    List<LinkjoyOfour>? snaps;
    switch (direction) {
      case ChatLoadDirection.normal:
        if (chatId > 0) {
          snaps = await LinkjoyAncestorOily.instance.querySnapList(chatId, 0);
          _hasForward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;

      case ChatLoadDirection.history:
        if (chatId > 0 && _hasForward) {
          LinkjoyOfour? old = oldestSnap();
          snaps = await LinkjoyAncestorOily.instance.querySnapList(
            chatId,
            old?.createTime ?? 0,
          );
          _hasForward = snaps != null && snaps.length >= PAGE_SIZE;
        }
        break;
    }

    if (snaps != null && snaps.isNotEmpty) {
      snaps.removeWhere((e) {
        for (var g in _groups) {
          if (g.snaps.contains(e)) return true;
        }
        return false;
      });

      LinkjoyAncestorOily.instance.linkjoyJazzHonStripper(snaps);

      List<LinkjoyAncestorOfourAholdKetchup>? groups =
          LinkjoyAncestorOfourAholdKetchup.sortSnaps(
            snaps,
            lastGroup: _groups.isEmpty ? null : _groups.last,
          );
      if (groups != null && groups.isNotEmpty) {
        switch (direction) {
          case ChatLoadDirection.normal:
            _groups.insertAll(0, groups);
            break;
          case ChatLoadDirection.history:
            _groups.addAll(groups);
            break;
        }
      }
    }
    _isLoading = false;
    bool loaded = snaps != null && snaps.isNotEmpty;
    if (loaded) {
      _linkjoyEskimoPersonnel();
    }
    return loaded;
  }

  Future<bool> _linkjoyElevenOfour(LinkjoyOfour? snap) async {
    if (snap == null) return false;

    if (snap.isGiftJsonSnap || _isSnapSendable()) {
      _linkjoyNetflixOfour(snap);
      CreateSnapRsp? rsp = await LinkjoyAncestorOily.sendQueue.sendSnap(snap);
      if (rsp == null) {
        return false;
      }

      if (rsp.code == 0) {
        if (chatId == 0) {
          chatId = rsp.chatboxId.toInt();

          await LINKJOY.linkjoyDb.chatBoxDao.updateChatBoxMember(
            oppositeUser ?? LinkjoyAncestorUnborn()
              ..uid = toUid
              ..nickName = toUserName
              ..avatarUrl = toUserAvtarUrl,
          );

          LINKJOY.eventBus.fire(
            LinkjoyAncestorGuitar(
              ChatEventType.chatBoxReloadByIds,
              chatIds: [chatId],
            ),
          );
        }

        linkjoyCanElevenOfour = true;
        linkjoyMarketingFoolishlyDeckIntolerant = false;
        update([linkjoy_vw_ancestor_phlegm]);
        return true;
      } else if (rsp.code == 6001) {
        LinkjoyUISocially.linkjoyAllenTeaserAnimal(
          fromType: LinkjoyWailImitateGnome.FROM_SEND_SNAP_COST_COINS,
          fromUid: toUid,
        );
      } else if (rsp.code == 206) {
        LinkjoyUpper.showError("linkjoy_caring_flashy_greeting_burt".tr);
      } else if (rsp.code == 403) {
        LinkjoyUIShredder.showSendImByVIPSheet(Get.context!, () {
          LINKJOY.toNamed(
            LinkjoyGet.LinkjoyPhillipsFailDivision,
            arguments: LinkjoyPhillipsStubbornJaw(
              fromType: LinkjoyWailImitateGnome.FROM_IM_NO_MORE_FREE_MSG,
            ),
          );
        });
      } else {
        LinkjoyUpper.showError(rsp.msg);
      }

      return false;
    } else {
      LinkjoyUIShredder.showSendImByVIPSheet(Get.context!, () {
        LINKJOY.toNamed(
          LinkjoyGet.LinkjoyPhillipsFailDivision,
          arguments: LinkjoyPhillipsStubbornJaw(
            fromType: LinkjoyWailImitateGnome.FROM_IM_NO_MORE_FREE_MSG,
          ),
        );
      });
      return false;
    }
  }

  bool linkjoyAmbitiousMarketingAncestorIntolerant() {
    if (LINKJOY.linkjoyShock.isLinkjoyAiLengthOgle()) return false;

    if (LINKJOY.linkjoyJog!.isLinkjoyFoolishly()) return false;

    if (oppositeUser == null) {
      return false;
    }

    if (oppositeUser!.temp == 100) return true;

    if (oppositeUser!.chatAuthExpire > DateTime.now().millisecondsSinceEpoch) {
      return false;
    }

    if (linkjoyCanElevenOfour) return false;

    int freeChatCard = LINKJOY.linkjoyUnbornDeposit?.cardIm ?? 0;
    if (freeChatCard > 0) {
      return true;
    }

    return false;
  }

  void _linkjoyNetflixOfour(LinkjoyOfour? snap) {
    if (snap == null) return;
    _linkjoyNetflixStripper([snap]);
  }

  void _linkjoyNetflixStripper(List<LinkjoyOfour> snaps) {
    if (LinkjoyGnomeSister.isEmptyList(snaps)) return;
    LinkjoyAncestorOily.instance.linkjoyJazzHonStripper(snaps);
    bool changed = false;

    snaps.removeWhere((e) {
      for (var g in _groups) {
        int index = g.snaps.indexOf(e);
        if (index > -1) {
          g.snaps.removeAt(index);
          g.snaps.insert(index, e);
          changed = true;
          return true;
        }
      }
      return false;
    });
    if (snaps.isNotEmpty) {
      final groups = LinkjoyAncestorOfourAholdKetchup.sortSnaps(
        snaps,
        lastGroup: _groups.isEmpty ? null : _groups.first,
        autoTranslate: translateMode,
      );

      if (!LinkjoyGnomeSister.isEmptyList(groups)) {
        _groups.insertAll(0, groups!);
      }
      changed = true;
    }
    if (changed) {
      _linkjoyEskimoPersonnel();
      update([linkjoy_vw_ancestor_stubborn]);
    }
  }

  void _linkjoyEskimoPersonnel() {
    List items = [];
    for (var e in _groups) {
      items.addAll(e.snaps);
      items.add(e.dateItem);
    }
    _items.clear();
    _items.addAll(items);

    if (!linkjoyCanElevenOfour && LINKJOY.isLinkjoyFoolishly) {
      for (var e in items) {
        if (e is LinkjoyOfour) {
          if (e.ownerId != LINKJOY.uid()) {
            linkjoyMarketingFoolishlyDeckIntolerant = true;
          } else {
            linkjoyMarketingFoolishlyDeckIntolerant = false;
          }
          break;
        }
      }
    }
  }

  void _linkjoyDreidelDuoStripper(LinkjoyAncestorGuitar event) {
    if (LinkjoyGnomeSister.isEmptyMap(event.affects)) return;
    List<LinkjoyOfour>? added;
    if (event.affects![LinkjoyAncestorGuitar.AFFECT_ADD] != null) {
      added =
          event.affects![LinkjoyAncestorGuitar.AFFECT_ADD]
              as List<LinkjoyOfour>?;
    }

    if (!LinkjoyGnomeSister.isEmptyList(added)) {
      _hasNewSnap = true;
      _linkjoyNetflixStripper(
        added!.where((e) => e.chatBoxId == chatId).toList(),
      );

      for (LinkjoyOfour snap in added) {
        if (snap.ownerId != LINKJOY.uid()) {
          LinkjoyAncestorOfourSpecificSkintight? jo = snap.jsonContentObj;
          if (jo != null && jo.isAnimationGift()) {
            giftsAnimationController.add(
              VoGift.fromSnapJson(jo),
              userName: toUserName,
              userAvatar: toUserAvtarUrl,
            );
          }
        }
      }
    }
  }

  void linkjoySimulationToSilly({bool animated = true, double? offset}) {
    _linkjoySimulationToZack(
      pos: LinkjoyAncestorSimulationZack.start,
      animated: animated,
      offset: offset,
    );
  }

  void _linkjoySimulationToZack({
    required LinkjoyAncestorSimulationZack pos,
    double? offset,
    bool animated = true,
  }) {
    WidgetsBinding.instance.addPostFrameCallback((timeStamp) {
      if (!scrollController.hasClients) return;

      if (pos == LinkjoyAncestorSimulationZack.offset) {
        offset = offset ?? 0;
      } else {
        if (scrollController.positions.isEmpty) {
          offset = offset ?? 0;
        } else {
          if (pos == LinkjoyAncestorSimulationZack.start) {
            offset = scrollController.positions.first.minScrollExtent;
          } else {
            offset =
                scrollController.positions.first.maxScrollExtent +
                (offset ?? 0);
          }
        }
      }

      if (animated) {
        scrollController.animateTo(
          offset!,
          duration: const Duration(milliseconds: 200),
          curve: Curves.ease,
        );
      } else {
        scrollController.jumpTo(offset!);
      }
    });
  }

  void _linkjoyHappilyAncestorUnbornSaver() async {
    LinkjoyAncestorUnborn? resp = await LINKJOY.http.rest(
      7011,
      {'uid': toUid},
      (p0) => LinkjoyAncestorUnborn.fromJson(p0),
      showLoadingUI: false,
    );
    if (resp != null) {
      oppositeUser = resp;
      toUserName = oppositeUser!.nickName;
      toUserAvtarUrl = oppositeUser!.avatarUrl;

      if (LinkjoyAncestorSister.isLinkjoySailorProcessing(toUid)) {
        oppositeUser!.temp = 100;
      }

      if (chatId > 0) {
        LINKJOY.linkjoyDb.chatBoxDao.updateChatTemp(chatId, oppositeUser!.temp);
      }
      await _updateUserIfNeed(oppositeUser!);
      update([
        linkjoy_vw_ancestor_unborn_saver,
        linkjoy_vw_ancestor_unborn_saver_decide,
        linkjoy_vw_ancestor_oops,
        linkjoy_vw_ancestor_phlegm,
      ]);
    }
  }

  Future<void> linkjoyHonBuzz() async {
    if (oppositeUser == null) return;

    var followStatus = oppositeUser!.isFollowed ? 0 : 1;
    Map<String, dynamic> params = {
      "uid": toUid,
      "source_type": 0,
      "status": followStatus,
    };
    bool resp = await LINKJOY.http.submit(
      3010,
      params,
      showLoadingUI: true,
      autoToastOnError: true,
    );
    if (resp) {
      oppositeUser?.followed = followStatus;
      LinkjoyUpper.showFollowToast(oppositeUser!.isFollowed);
      LINKJOY.fire(LinkjoyUnbornProcreateGuitar(toUid, followStatus));
    }
  }

  void onLinkjoySheetSilliness() async {
    bool rsp = await LINKJOY.http.submit(
      4010,
      {"uid": toUid},
      showLoadingUI: true,
      autoToastOnError: true,
    );

    if (rsp) {
      LinkjoyUpper.showSuccess('linkjoy_caring_bronze_soda'.tr);
      LINKJOY.fire(
        LinkjoyPearlVulcanGuitar(
          scene: LinkjoyPearlVulcanGuitarDollhouse.force_refresh,
        ),
      );
      LINKJOY.fire(LinkjoyHomelandAncestorSupercoolGuitar(chatBoxId: chatId));
      Get.back();
    }
  }

  void _linkjoyNarrateInspectorNosy() {
    _linkjoyElevenEskimoFbiHair();
  }

  void _linkjoyElevenEskimoFbiHair() {
    if (chatId > 0) {
      LinkjoyAncestorOily.instance.linkjoyAncestorStripperPatterHair(chatId);
    }
  }

  void onLinkjoyWizardAndrew() {
    LinkjoyGet.openProfile(
      toUid,
      nickName: toUserName,
      avatarUrl: toUserAvtarUrl,
    );
  }

  void translate() {
    translateMode = !translateMode;
    update([linkjoy_vw_ancestor_pancake]);
  }

  bool _isSnapSendable() {
    if (LINKJOY.linkjoyJog!.isLinkjoyFoolishly()) return true;

    if (LINKJOY.linkjoyShock.isLinkjoyAiLengthOgle()) return true;

    if (oppositeUser == null) {
      return true;
    }

    if (oppositeUser!.temp >= 100) {
      return true;
    }

    if (oppositeUser!.chatAuthExpire > DateTime.now().millisecondsSinceEpoch) {
      return true;
    }

    if (linkjoyCanElevenOfour) {
      return true;
    }

    int freeChatCard = LINKJOY.linkjoyUnbornDeposit?.cardIm ?? 0;
    return freeChatCard > 0;
  }

  Future<void> _updateUserIfNeed(LinkjoyAncestorUnborn oppositeUser) async {
    final existingUser = await LINKJOY.linkjoyDb.userDao.modelById(
      oppositeUser.uid,
    );

    if (existingUser != null) {
      bool nickNameChanged = existingUser.nickName != oppositeUser.nickName;
      bool avatarUrlChanged = existingUser.avatarUrl != oppositeUser.avatarUrl;

      if (nickNameChanged || avatarUrlChanged) {
        await LinkjoyAncestorOily.instance
            .linkjoyEskimoAncestorSupercoolTrainer(oppositeUser);
      }
    }
  }

  void linkjoyDoubtElevenFlashy() {
    isShowGiftShop = !isShowGiftShop;
    update([linkjoy_flashy_polish]);
  }

  Future<void> linkjoyElevenFlashyWarlord(
    VoGift gift, {
    bool beg = false,
  }) async {
    if (LINKJOY.isLinkjoyFoolishly) {
    } else {
      if (LINKJOY.linkjoyUnbornDeposit!.balance < gift.cost) {
        LinkjoyUISocially.linkjoyAllenTeaserAnimal(
          fromType: LinkjoyWailImitateGnome.FROM_SEND_SNAP_COST_COINS,
          fromUid: toUid,
        );
        return;
      }

      if (!beg) {
        if (gift.type == 2 && !LINKJOY.isVip()) {
          LinkjoyUIShredder.showSendImByVIPSheet(Get.context!, () {
            LINKJOY.toNamed(
              LinkjoyGet.LinkjoyPhillipsFailDivision,
              arguments: LinkjoyPhillipsStubbornJaw(
                fromType: LinkjoyWailImitateGnome.FROM_IM_SEND_GIFT,
              ),
            );
          });
          return;
        }
      }
    }

    linkjoyDoubtElevenFlashy();
    bool succ = await linkjoyElevenFlashySatellite(gift);
    if (succ) {
      if (!LINKJOY.isLinkjoyFoolishly) {
        giftsAnimationController.add(
          gift,
          userName: "linkjoy_caring_me".tr,
          userAvatar: LINKJOY.user().avatarUrl,
        );
      }
    }
  }

  void _linkjoyElevenHairEskimo(LinkjoyOfour localSnap) {
    LinkjoyAncestorOily.instance.linkjoyJazzHonStripper([localSnap]);
    bool changed = false;
    for (var g in _groups) {
      int index = g.snaps.indexWhere((f) => f.localId == localSnap.localId);
      if (index > -1) {
        g.snaps.removeAt(index);
        g.snaps.insert(index, localSnap);
        changed = true;
        break;
      }
    }
    if (changed) {
      update([linkjoy_vw_ancestor_stubborn]);
      linkjoySimulationToSilly(animated: true);
    }
  }

  String get toName =>
      oppositeUser == null ? toUserName : oppositeUser!.nickName;

  String get toAvatar {
    if (LinkjoyAncestorSister.isLinkjoySailorProcessing(toUid)) {
      return LinkjoyAncestorSister.linkjoy_sailor_vent;
    }

    return oppositeUser == null ? toUserAvtarUrl : oppositeUser!.avatarUrl;
  }

  String linkjoyAncestorOopsIntolerant() {
    String text = "linkjoy_caring_flashy_oops_limbic".tr.replaceFirst(
      "%s",
      "${oppositeUser!.temp}",
    );
    text = text.replaceFirst(
      "%s",
      "${oppositeUser!.temp == 100 ? 0 : (100 - oppositeUser!.temp)}",
    );
    return text;
  }
}
