import 'package:flutter/material.dart';

class LinkjoyMealSister {
  static BorderRadius get smallRadius => BorderRadius.circular(4);

  static BorderRadius get mediumRadius => BorderRadius.circular(8);

  static BorderRadius get largeRadius => BorderRadius.circular(16);

  static BorderRadius get fullRadius => BorderRadius.circular(100);
}
